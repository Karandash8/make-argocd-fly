---
apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: grafana
  name: grafana
  namespace: kube-monitoring
spec:
  selector:
    k8s-app: grafana
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
      nodePort: 30000
  type: NodePort
