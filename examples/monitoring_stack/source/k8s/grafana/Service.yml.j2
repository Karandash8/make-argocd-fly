apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: grafana
  name: grafana
  namespace: {{ monitoring.namespace }}
spec:
  selector:
    k8s-app: grafana
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
      nodePort: {{ grafana_node_port }}
  type: NodePort
